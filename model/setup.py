from distutils.core import setup

print("This requires torch (pytorch) so install it if you don't have it already")
setup(
    name="model",
    version="0.0",
    description="Language Modelling Experiments",
    author="Edward Newell"
)

